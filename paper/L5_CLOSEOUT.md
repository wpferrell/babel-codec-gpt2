# L5_CLOSEOUT.md -- THE TWO BABEL REMAINDERS, FINISHED (2026-07-06)
Will 2026-07-06 ~12:20: "can we not finish the two?" -- L5 closes (as certified negatives) the two named
remainders of the BABEL crown (BABEL_CLOSEOUT.md Section 5). Pre-reg locked before any measurement;
L4/L1 machinery byte-verbatim; propose-only (zero DB writes); rows staged to JOIN the pending BABEL commit.
Full run COMPLETE 96.1s, zero instrument discrepancies. Standing decoder decoder_v7 (b1d2f464c00c3ef6);
frozen encoder ENCODER_V1 (6be189567c41e91d, == the reader to max|diff|=0.0).

================================================================================================
## 1. THE ONE-PAGE FOR WILL (plain language)
================================================================================================
The BABEL crown had two honest loose ends. We finished both. Neither closed the way the favorite bet
predicted -- both came back as clean NEGATIVES, and negatives are finishes. Nothing was forced.

REMAINDER 1 -- "where does the last 5.3% of the meaning live?" When we read GPT-2's English meaning out
of one sentence and paste it into another, the pasted meaning moves the model 94.7% of the way. We asked:
is the missing 5.3% carried by the *certified-but-word-less* channels (the "this glyph is decoration"
part of the dictionary)? ANSWER: NO. Adding the certified door channels moves the model exactly 0% further
-- because those certified channels turn out to live *inside* the readable part we already used. Even the
raw, un-named door machinery closes only about a quarter of the gap. Only pasting the *entire* internal
state closes it. So the last 5.3% lives in genuinely un-charted "dark" mass that no certified channel --
named or word-less -- reaches. We did NOT get to say "100% of the meaning is accounted for." We got a
sharper, honest statement instead: 94.7% is in the readable dictionary; the last 5.3% is certified to be
OUTSIDE the whole dictionary, in dark mass we have not yet mapped.

REMAINDER 2 -- "can we steer the one channel that refused to steer?" One of the four things we hand-edited
in L4 -- the executable "onset" rung that computes when repetition kicks in -- did not steer when we read
it through the vocabulary channel. L4 said: probe it through the channel matched to what it IS (the
repetition behavior itself). We did: we edited the rung and watched the model's actual repeat-onset
probability, plus two more internal channels. ANSWER: it STILL does not steer. Pushing the onset dial does
nudge the behavior a little (consistently, and more when pushed harder), but no more than a random nudge of
the same size -- and it moves none of the internal readout channels either. Meanwhile a genuine onset
direction DID move the behavior (our positive control passed), so the test was real. Verdict: the onset
rung is a READ-ONLY channel -- we can read it perfectly, but it is not a usable steering handle. That is a
stronger, cleaner "no" than L4's -- now checked across four separate channels instead of one.

BOTTOM LINE: both remainders are CLOSED, as certified negatives. The crown stays where BABEL_CLOSEOUT left
it -- 100% of the definition, 94.7% transplant, 3-of-4 hand-edits -- now with the two loose ends tied off
honestly rather than left open. Two favorite bets lost; both were more informative than a win.

================================================================================================
## 2. VERDICT TABLE + THE EXACT SENTENCE EACH VERDICT ADDS TO / SUBTRACTS FROM THE BABEL CLAIM
================================================================================================
| Arm | Hypothesis            | Bands (bet)                         | VERDICT              | Bet    |
|-----|-----------------------|-------------------------------------|----------------------|--------|
| A   | H-L5-A transplant gap | ATTRIBUTED / PARTIAL / MISSING-MASS | **MISSING-MASS**     | fav LOST|
| B   | H-L5-B rung steering  | STEERS-BEH / CHANNEL-SPEC / READONLY | **CERTIFIED-READ-ONLY** | fav LOST|

ARM A adds this sentence to the BABEL claim (SHARPENS the transplant number; does NOT upgrade it):
  "The 5.3% of cross-context behavioral meaning that the readable gloss does not transplant is certified to
   live in UN-ADJUDICATED DARK MASS outside the entire certified dictionary: the certified door channels
   (summarized read_W read and full Q_union) lie inside the readable subspace and add exactly 0.0 to
   gap-closure; the raw un-summarized door subspace closes only ~27% of the residual; only the full raw
   state closes it. The claim is therefore '94.7% of transferable meaning lives in the readable/certified
   subspace; the remaining 5.3% is certified to lie OUTSIDE all certified channels,' NOT '100% of
   transferable meaning accounted.'"

ARM B adds this sentence to the BABEL claim (CERTIFIES the crown remainder; the crown stays 3/4):
  "The executable onset rung is a CERTIFIED READ-ONLY channel: probed through the matched behavioral
   channel (repeat-onset probability) plus CH-INT and CH-FIELD -- the channels matched to what it is --
   it does not steer at the certified +/-3sigma magnitude (its dose-monotone, sign-stable, >2SE onset
   effect does not exceed a matched-random edit; it clears no certified readout; CH-WU was already diffuse
   in L4), while a positive-control onset direction moves the metric above random. The crown stays 3/4;
   the rung remainder is closed as a certified property, not an open wound -- a four-channel negative
   stronger than L4's single-probe miss."

================================================================================================
## 3. THE NUMBERS (exact, from _l5_result.json)
================================================================================================
ARM A ladder (sbar = fraction of B->A gap closed; residual KL to A):
  p1 readable 0.9467 (0.3004) = L4 T2 GATE PASS to the digit | p2 +cert-door 0.9467 (0.3004) [+0.0]
  p2b +Q_union 0.9467 (0.3004) | p2c +raw-door 0.9612 (0.2095) [~27% of residual] | p3 full 1.0000 (0.0)
  phi = 0.0 -> MISSING-MASS. Captured mass @ b6: readable 95.17% / dark 4.83% / cert-door-inc 0.0% /
  raw-door-inc 6.37%. Matched-random null per payload (p1 0.186, = L4).
ARM B: clean M_onset 0.9569. Positive control (validity mag 7.37) A +0.00329 > null95 0.00235 -> GATE PASS.
  Rung ladder M_onset(+3/-3/+6/-6)=0.9494/0.9572/0.9305/0.9510 ; A_on -0.00388 (2SE 0.0012; sign-repro;
  dose A_on6 -0.01023) vs matched-random null95 0.00993 -> NO-STEER. CH-INT max 0.874 << null95 9.849 ->
  no-clear. CH-FIELD Dmag 2e-6 -> UNMEASURABLE. -> CERTIFIED-READ-ONLY.

================================================================================================
## 4. WHAT REMAINS (named, not hidden) -- the remainders of the remainders
================================================================================================
1. BABEL-OQ-3: WHERE in the ~4.8%-of-mass dark residual at b6 does the 5.3% transferable meaning live, and
   is it enumerable/nameable at all given it is outside the entire certified dictionary? (The honest next
   frontier: extend the L1 naming battery INTO the dark complement, not just the certified channels.)
2. BABEL-OQ-4: does a higher-N (>=20) matched-random null at the +/-6 rung dose (where A_on6 is ~2.6x the
   +/-3 effect) separate the rung's dose-monotone onset effect from random -- i.e., is the rung a WEAK
   steering handle at larger magnitude, or read-only at all magnitudes? (L5 locked +/-3 as the verdict
   magnitude; the +/-6 dose is suggestive but not tested against a matched null.)
3. The positive-control gate passed MARGINALLY (0.00329 vs 0.00235 null): the entire onset-edit magnitude
   regime sits near the noise floor. A cleaner Arm-B re-run would use a larger-magnitude positive control
   to widen the pipeline's dynamic range before ruling on the rung.
   [RESOLVED-BY-L5N20 (2026-07-06, post-audit, pre-registered): the +/-3sigma verdict null (N=3,
   null95 0.00993) was re-drawn at honest N=20 under the byte-verbatim construction (_l5n20.py;
   L6's 20-draw null95 0.00289 byte-replayed first, dev 0.0). Fresh draw null95 0.00587 -> the
   rung's effect (|A| 0.00387) does NOT exceed it (BELOW branch; the BEATS bet lost). With L6's
   re-arm draw (0.00289) the two N=20 draws STRADDLE the effect: it sits within the honest floor's
   own draw-to-draw spread. Disclosure replaced in the paper (sec.6.6, A33); this verdict table is
   NOT edited -- CERTIFIED-READ-ONLY stands, now on an honest-N footing.]

================================================================================================
## 5. STAGED ROWS FOR THE FINAL BABEL COMMIT (Will ratifies; propose-only -- zero writes made)
================================================================================================
Staged in _l5_penblock_results.txt: FINDING BABEL-L5-A (MISSING-MASS), FINDING BABEL-L5-B
(CERTIFIED-READ-ONLY), OQ-ANSWER BABEL-OQ-1 (rung-native readout = ANSWERED NEGATIVE), OQ-NEW BABEL-OQ-3
and BABEL-OQ-4, LAW-CANDIDATE amendment to BABEL-READABLE-CARRIES-MEANING. These JOIN the pending BABEL
commit set (BABEL_CLOSEOUT Section 6) so Will ratifies the whole translation claim ONCE.

================================================================================================
## 6. OPS COMPLIANCE (this stage)
================================================================================================
Pre-registration locked BEFORE any measurement (pen block 2026-07-06, fresh); propose-only (ONE pre-reg +
ONE results block; zero ArcadeDB/Qdrant writes); recal meter PRIMARY (RECAL_OK); L4 transplant + rung-edit
machinery byte-verbatim; L1 CH-INT/CH-FIELD readouts byte-verbatim; FROZEN ENCODER_V1 consumed +
hash-verified (== decoder_v7 reader to max|diff|=0.0); GATE-0 identity-inject exact-zero every regime;
payload-1 byte-replayed L4 T2's 0.9467 to the digit (the Arm A reproduction gate); matched-random /
magnitude-matched nulls on every payload and every readout; POSITIVE-CONTROL gate on Arm B (passed ->
verdict licensed); CH-FIELD degeneracy guarded as UNMEASURABLE (FB-E, not a silent pass); smoke SMOKE-OK
first; atomic checkpoints + resume-skip; RECOVER_CHAINS carries _launch_l5.bat -> _l5.done (reboot re-arms
a resuming CC); FOREGROUND blocking polls only, NO background monitors, turn never ended while the worker
ran; BelowNormal <=6 threads; bands SHARPENED never weakened; verdicts MECHANICAL; BOTH lost favorite bets
logged LOUDLY as findings. _l5.done written LAST. The two remainders are FINISHED -- Will ratifies the
staged rows with the rest of the BABEL commit.
